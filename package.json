{
  "name": "mountainrun",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "frontend:dev": "npm run dev --prefix frontend",
    "frontend:build": "npm run build --prefix frontend",
    "frontend:start": "npm run start --prefix frontend",
    "frontend:lint": "npm run lint --prefix frontend",
    "api:dev": "npm run dev --prefix backend",
    "api:build": "npm run build --prefix backend",
    "api:prisma:generate": "npm run prisma:generate --prefix backend",
    "api:prisma:migrate": "npm run prisma:migrate --prefix backend",
    "api:prisma:seed": "npm run prisma:seed --prefix backend"
  }
}
